import { CardData, KeyId, DecodedJwtData, Payload, Header, InitializationVector } from '../types';
export declare const CardTypes: {
    Visa: string;
    MasterCard: string;
    AmericanExpress: string;
    Discover: string;
    Diners: string;
    JCB: string;
    Maestro: string;
    ChinaUnionPay: string;
};
export declare const encrypt: (data: CardData, context: string, index?: number) => Promise<string>;
export { CardData, KeyId, DecodedJwtData, Payload, Header, InitializationVector };
